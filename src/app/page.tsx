// Root page - since we're using (dashboard) route group,
// this file can be deleted and the dashboard will serve at /
// Keeping it here as a placeholder for a future landing page

export default function HomePage() {
  return null; // Dashboard layout handles everything via route group
}
